import React from 'react';

const fallback = () => (
    <div>
        <h1>You are offline. We can sync up later.</h1>
        <small>Please try again later when you have reconnected.</small>
    </div>
);

export default fallback;
